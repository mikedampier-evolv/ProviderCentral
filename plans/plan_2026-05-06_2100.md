---
created: 2026-05-06T21:00:08.228Z
session: 60975781-8546-4598-913c-ab99f47bffd7
working_directory: /Users/mike.dampier/Development/Provider360
---

## Context

The Ops Monitor page (`pages/9_Ops_Monitor.py`) currently has 4 sections: Pipeline Log, Data Quality, Model Performance, and Platform Inventory, plus a Task DAG table. There's no visual lineage graph.

The app uses **Plotly** (5.18.0) — no `networkx` or `graphviz` available. The DAG will be built using `go.Scatter` for nodes and line annotations for edges, which is a standard Plotly network-graph pattern.

The lineage flows through 5 layers:
```
Dimensions → Facts → Marts → Feature Views → Predictions
```

## Implementation Steps

**1. Add a "Data Lineage DAG" section** to `pages/9_Ops_Monitor.py` (insert after the Platform Inventory section, before the existing Task DAG section):

- Define nodes with (x, y) positions arranged in 5 columns (left to right):
  - **Col 1 — Dimensions**: DIM_PATIENT, DIM_PROVIDER, DIM_FACILITY, DIM_PAYER, DIM_DRG, DIM_PROCEDURE_CPT, DIM_DIAGNOSIS_ICD10, DIM_DEPARTMENT
  - **Col 2 — Facts**: FCT_ENCOUNTER, FCT_REFERRAL, FCT_OR_CASE, FCT_CLAIM_LINE
  - **Col 3 — Marts**: MART_READMISSION_LOS, MART_PATIENT_LEAKAGE, MART_OR_CAPACITY, MART_DENIALS_REVCYCLE
  - **Col 4 — Feature Views**: VW_DAILY_ENCOUNTER_VOLUME, VW_DAILY_DENIAL_VOLUME, VW_READMISSION_DRIVERS, VW_LEAKAGE_DRIVERS
  - **Col 5 — Predictions**: PRED_ENCOUNTER_VOLUME, PRED_DENIAL_ANOMALIES, PRED_READMISSION_DRIVERS, PRED_LEAKAGE_DRIVERS

- Draw edges as `go.Scatter` traces with mode="lines" connecting:
  - Dims → Facts (FK relationships)
  - Facts + Dims → Marts (join lineage)
  - Marts → Feature Views (aggregation)
  - Feature Views → Predictions (ML output)

- Color-code nodes by layer using `chart_theme.COLORS`
- Apply the dark theme via `ct.apply_style()`
- Add a legend explaining the color coding for each layer

**2. Style details**:
- Nodes as `go.Scatter` markers (circles, size ~20-25)
- Edges as separate `go.Scatter` lines (thin, semi-transparent)
- Node labels using text mode
- Height ~600px for readability
- Hover shows full table name and row count

## Verification

- Visually confirm the DAG renders with distinct layers and connected edges
- Upload to stage, redeploy, refresh the Ops Monitor page
- Confirm node labels are readable on dark background
- Confirm hover text shows table details

## Critical Files

- `streamlit/pages/9_Ops_Monitor.py` — Add the lineage DAG section
- `streamlit/chart_theme.py` — Reference for color constants and `apply_style()` helper (read-only)
- `streamlit/requirements.txt` — Confirm no new dependencies needed (Plotly already there)