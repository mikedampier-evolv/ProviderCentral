---
created: 2026-05-08T15:57:00.340Z
session: fd779caf-f47c-47ac-9657-5c6f7c2ff41d
working_directory: /Users/mike.dampier/Development/Provider360
---

## Context

The demo has `FCT_LABOR_HOUR` (730K rows, per employee-shift with DEPT_ID, FACILITY_ID, SHIFT_DATE, WORKED_HRS, OT_HRS, HOURLY_RATE, TOTAL_LABOR_COST) and `FCT_ENCOUNTER` / `MART_READMISSION_LOS` (clinical outcomes with DEPT_ID, FACILITY_ID, ADMIT_DATE). The join key is **department + date** (`DEPT_ID` + `SHIFT_DATE`/`ADMIT_DATE`). I'll follow the established pattern: DDL → seed → Streamlit page → Home.py section → deploy.

## Use Case: Staffing & Quality (UC5)

**Question answered**: "Do understaffed units have higher readmission rates and longer length-of-stay?"

**Mart grain**: 1 row per department per day — aggregates labor hours and clinical outcomes to enable correlation analysis.

## Implementation Steps

### 1. Add mart DDL to `infrastructure/11_mart_ddls.sql`

Create `HOSPITAL360_CUR.WORKFORCE.MART_STAFFING_QUALITY` with columns:
- `DEPT_ID`, `DEPT_NAME`, `DEPT_TYPE`, `FACILITY_ID`, `FACILITY_NAME`
- `SHIFT_DATE`, `SHIFT_MONTH`
- **Labor metrics**: `TOTAL_WORKED_HRS`, `TOTAL_OT_HRS`, `OT_PCT`, `TOTAL_LABOR_COST`, `STAFF_COUNT`, `AGENCY_STAFF_COUNT`, `AGENCY_PCT`
- **Patient metrics**: `PATIENT_CENSUS` (encounters active that day), `HRS_PER_PATIENT`, `ADMITS`, `DISCHARGES`
- **Quality metrics**: `READMIT_COUNT`, `READMIT_RATE`, `AVG_LOS_DAYS`, `AVG_LOS_INDEX`, `LONG_STAY_COUNT`
- **Flags**: `IS_UNDERSTAFFED` (HRS_PER_PATIENT below department-type benchmark), `HIGH_OT_FLAG` (OT_PCT > 15%)

### 2. Add seed logic to `infrastructure/12_seed_marts.sql`

INSERT combining `FCT_LABOR_HOUR` (grouped by DEPT_ID + SHIFT_DATE) with `FCT_ENCOUNTER` outcomes (grouped by DEPT_ID + ADMIT_DATE), joined to `DIM_DEPARTMENT` and `DIM_FACILITY`.

### 3. Create Streamlit page `streamlit/pages/5_Staffing_Quality.py`

Following the existing page pattern (filters, KPIs, charts):
- **KPIs**: Avg Hrs/Patient, Overtime %, Understaffed Days %, Readmit Rate (understaffed vs. adequately staffed)
- **Chart 1**: Monthly trend — Hrs/Patient vs. Readmission Rate (dual-axis)
- **Chart 2**: Department heatmap — staffing level by day of week
- **Chart 3**: Scatter plot — Hrs/Patient (x) vs. Readmit Rate (y) by department
- **Chart 4**: Bar chart — Overtime % by department, colored by readmission rate
- **Detail table**: Department-day level with staffing and quality metrics

### 4. Renumber existing pages 5-8

- `5_Denials_RevCycle.py` → `6_Denials_RevCycle.py`
- `6_Predictive_Analytics.py` → `7_Predictive_Analytics.py`
- `7_Provider_Chat.py` → `8_Provider_Chat.py`
- `8_Ops_Monitor.py` → `9_Ops_Monitor.py`

New page: `5_Staffing_Quality.py`

### 5. Update `Home.py`

- Add section "5. Staffing & Quality" talk track (between OR Capacity and Denials)
- Renumber sections 5→6 (Denials), 6→7 (Predictive Analytics), 7→8 (Provider Chat), 8→9 (Ops Monitor)
- Update all `📂 Navigate via sidebar` captions with new names

### 6. Update `pages/1_Provider_360.py`

- Update page_link references for renumbered pages
- Add a staffing KPI or link card

### 7. Deploy to Snowflake

- Execute DDL + seed SQL
- Upload all renamed/new page files to stage
- Recreate Streamlit and re-grant access

## Verification

- Run the mart DDL and seed SQL; confirm row count > 0
- Verify Streamlit loads with sidebar showing: Provider 360, Readmission & LOS, Patient Leakage, OR Capacity, **Staffing & Quality**, Denials & RevCycle, Predictive Analytics, Provider Chat, Ops Monitor
- Spot-check mart data: understaffed days should correlate with slightly higher readmission rates (since we're generating synthetic data, we'll add a bias)

## Critical Files

- `infrastructure/11_mart_ddls.sql` — Add MART_STAFFING_QUALITY DDL
- `infrastructure/12_seed_marts.sql` — Add UC5 seed INSERT logic
- `streamlit/pages/5_Staffing_Quality.py` — New Streamlit page (pattern from 4_OR_Capacity.py)
- `streamlit/Home.py` — Add talk track section, renumber subsequent sections
- `streamlit/pages/1_Provider_360.py` — Update page links for renumbered pages